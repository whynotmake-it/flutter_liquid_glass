name: apple_match_flutter
description: Deterministic Flutter capture target for Apple liquid-glass matching.
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  liquid_glass_renderer:
    path: ../../../..

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
