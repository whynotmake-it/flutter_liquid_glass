name: blur_progressive_example
description: Comparison of progressive blur implementations.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  blur_progressive:
    path: ../
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  progressive_blur: ^0.0.2
  soft_edge_blur: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
